import 'package:flutter/material.dart';

class MapsListPage extends StatelessWidget {
  const MapsListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Maps list page'),
    );
  }
}
